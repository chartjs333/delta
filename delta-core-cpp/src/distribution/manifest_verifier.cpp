#include <delta/distribution/manifest_verifier.hpp>

// The manifest verifier and certification policy are deliberately one native semantic boundary.
// This translation unit remains explicit so build manifests can bind both owned artifacts.

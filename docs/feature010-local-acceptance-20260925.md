# September 25: agreed local acceptance scope

The user granted four hours plus the night and reported that the receiving side
accepts synthetic participants and Docker networking instead of independent real
authorities and real WAN. Record an explicit control report by **19:05 UTC on
September 25, 2026**. Full Feature010 GO by morning was not promised.

This authorizes a separately labeled **SIMULATED_LOCAL** acceptance profile.
It does not change the original qualifying BenchmarkResultQC/GO definition.
An eventual local PASS must name its exact scope, source, environment, inputs,
measured runs and failed/open checks. Unexecuted or missing evidence cannot pass.

## Substitutions accepted

- Local synthetic participants with test identities and signing keys, explicitly
  under one administrator; no independent custody claim.
- Docker network/process failures and configured simulated delay/loss in place
  of real WAN endpoints; evidence must say SIMULATED, not real WAN or regions.

## Conditions retained

- Exact formal authority before changing native arithmetic admission. The current
  amendment remains NO_GO and native guards stay in place.
- Actual authoritative PARAMETER/APPLY recomputation by native C++ before WAL,
  persist-before-expose, original bytes/receipt/sequence on retry and recovery,
  deterministic conflict without another append.
- Actual joined Python → Java/Netty → C++/WAL → result lineage when claiming a
  full execution. Separate baseline training, Python arithmetic reference and
  Docker quorum examples cannot be relabeled as one joined run.
- Exact profile/environment/source identities and appropriate real execution
  checks. Physical GPU training cannot be inferred from a GPU visibility card.
- Honest verification reports. Self-review is permitted for the work but is not
  an independent formal attestation or a replacement for unfulfilled formal gates.

No local acceptance PASS, qualifying BenchmarkResultQC or Feature010 GO checkpoint
is issued by this planning document. The saved working demonstration remains the
fallback; it retains its existing scope. Preserve its frozen refs and healthy
services until a tested replacement is ready.

# Hybrid Runtime Tasks: Feature 010 Polyglot Benchmark

Historical completion marks are not inherited. HR010-001–003 are complete only as
current-lineage foundation identity/admission/evidence obligations at
`6054e35ed627392172f94c2a90dfb7d799d5251e`. These marks do not qualify a runtime
lane, authorize execution, create primary evidence, or satisfy HR010-004+.

## Identity and environment

- [x] **HR010-001** Bind BenchmarkDefinition to exact C++/ABI/Java/Python/build,
  formal-semantics and deployment-profile identities.
- [x] **HR010-002** Freeze planned compiler/JDK/Python/Netty/native/CUDA identities
  and the canonical fixture corpus before any authorized results.
- [x] **HR010-003** Add runtime compatibility admission and fixture evidence capture.

## Exact cross-language gates

- [ ] **HR010-004** Run Python/C++/Java exact canonical-byte, hash, status and
  negative-parsing corpus.
- [ ] **HR010-005** Run GCC/Clang and supported-architecture exact state/effect
  comparison.
- [ ] **HR010-006** Run direct-versus-copy FFM and flat-versus-hierarchy exact
  comparison.
- [ ] **HR010-007** Run complete implementation trace projection/refinement and
  production-mutant regression.

## Safety and failure gates

- [ ] **HR010-008** Execute ASan/UBSan, separate TSan and parser-fuzz campaigns at
  preregistered bounds.
- [ ] **HR010-009** Execute WAL/durability/effect/current crash matrix and verify
  replay identity.
- [ ] **HR010-010** Execute Netty leak, event-loop block, backpressure, stream-bound
  and stale-timer matrix.
- [ ] **HR010-011** Execute ABI/schema/formal-semantics mismatch and native pointer-
  lifetime negatives.

## Embedded versus sidecar

- [ ] **HR010-012** Implement/freeze isolated-sidecar IPC or preregister a formal
  risk decision for omission.
- [ ] **HR010-013** Compare embedded/native-process crash containment,
  restart/replay, latency and throughput.
- [ ] **HR010-014** Select the pilot validator profile through immutable benchmark
  evidence.

## Reporting

- [ ] **HR010-015** Report zero-copy eligibility/hit rate, copy-fallback bytes and
  phase-latency decomposition.
- [ ] **HR010-016** Require every primary DeltaReduce result to carry one joined
  Python-training → Java/Netty-transport → native-validation/WAL/effect receipt
  lineage, then join that lineage to token/domain-matched science and WAN/P2P
  evidence. Independent Python-only and conformance-only results cannot be joined
  post hoc.
- [ ] **HR010-017** Make any failed mandatory runtime/formal gate force
  deterministic `NO_GO`.
- [ ] **HR010-018** Publish complete offline-verifiable polyglot evidence and
  BenchmarkResultQC.

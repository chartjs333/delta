# delta-worker-python

Python 3.12 worker-local scientific runtime. Feature 001 owns deterministic baseline training,
artifact contracts and offline WAN simulation here. Consensus state, transport and native ABI
logic are forbidden in this component.

The package consumes `delta-protocol` canonical data contracts but does not define protocol bytes
from Python object layout.
